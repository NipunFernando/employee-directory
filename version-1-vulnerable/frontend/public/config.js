// Choreo will overwrite this in the deployed environment.
// This local placeholder prevents a SyntaxError when the dev server serves index.html for unknown paths.
window.configs = window.configs || {};

